<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:local="clr-namespace:GoodWorkersMVP.ViewModels"
             BindingContext="{Binding Main, Source={StaticResource Locator}}"
             x:Class="GoodWorkersMVP.Pages.ProfilePage">
    <StackLayout BindingContext="{Binding Profile}" Padding="10">

        <Image Source="{Binding User.ProfileImage}" Aspect="Fill" WidthRequest="200" HeightRequest="200"></Image>
        
        <Label Text="{Binding User.FullName}" FontSize="Title" HorizontalOptions="CenterAndExpand"></Label>
        <Label Text="{Binding User.AboutMe}" FontSize="Subtitle" HorizontalOptions="CenterAndExpand"></Label>

        <StackLayout Grid.Row="0" VerticalOptions="End" WidthRequest="200">
            <Frame CornerRadius="20" BackgroundColor="White">
                <Label Text="Hola que tal, soy el chico de las poesias, tu fiel admirador, y aunque no me conocias... hoy es noche de sexo........"></Label>
            </Frame>
        </StackLayout>

        <Grid Margin="0,0,0,15">
            <Grid.RowDefinitions>
                <RowDefinition Height="70" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Grid.Row="0" Text="Calificar y Comentar" 
               BackgroundColor="#18bc9c"
               TextColor="White"
               CornerRadius="10"
               HorizontalOptions="FillAndExpand"></Button>

            <Button Grid.Column="1" Grid.Row="0" Text="Enviar Mensaje"
                BackgroundColor="#3498db"
                TextColor="White"
                CornerRadius="10"
                HorizontalOptions="FillAndExpand"></Button>
        </Grid>

        <Label Text="Opiniones" FontSize="Title" Margin="0,0,0,10"></Label>
        <Label Text="Trabajos Anteriores" FontSize="Title" Margin="0,0,0,10"></Label>
        
    </StackLayout>
</ContentPage>